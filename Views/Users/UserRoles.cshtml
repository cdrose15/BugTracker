@model BugTracker.Models.AssignRolesViewModel 

@*@using (Html.BeginForm())
{
    <div class="form-horizontal">
        @Html.ListBox("SelectedRoles", Model.Roles, new { @id = "multi" })
    </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
}

<div>
    @Html.ActionLink("Back", "Users")
</div>*@

<div class="container">
    <div class="col-md-12">
        <div class="mypage-header panel-heading">
            <h3>Manage Roles for @Model.FirstName @Model.LastName</h3>
        </div>
        <div class="row mypage-header">
            @Html.ActionLink("Back", "Users")
        </div>
    </div>

    <div class="col-md-6">
        <div>
            <div class="panel-heading">
                <h4>Assign Roles</h4>
            </div>
            @using (Html.BeginForm())
            {
                <div class="form-group">
                    <div class="col-md-6">
                        @Html.ListBox("SelectedRoles", Model.Roles, new { @id = "multi" })
                    </div>
                    <div class="col-md-4">
                        <input type="submit" value="Save" class="btn btn-default" />
                    </div>
                </div>
            }
        </div>
    </div>
 </div>

    @section scripts{
        <script src="~/Scripts/bootstrap-multiselect.js"></script>

        <script type="text/javascript">
            $(document).ready(function() {
                $('#multi').multiselect({
                    buttonWidth: '240px'
                });
            });
        </script>

    }
